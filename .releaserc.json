{
  "branches": ["master"],
  "plugins": [
    [
      "@semantic-release/commit-analyzer",
      {
        "preset": "eslint",
        "releaseRules": [
          { "message": "FC", "release": "patch" },
          { "message": "*README*", "release": "patch" },
          { "message": "fc", "release": "patch" }
        ]
      }
    ],
    [
      "@semantic-release/release-notes-generator",
      {
        "preset": "eslint",
        "presetConfig": {
          "types": [
              {"message": "*FC*", "section": "Task" },
              {"message": "*fc*", "section": "Task" }
          ],
          "parserOpts": {
            "noteKeywords": ["FC", "fc"]
          },
          "issuePrefixes": ["FC"],
          "commitUrlFormat": "{{host}}/{{owner}}/{{repository}}/commits/{{hash}}",
          "compareUrlFormat": "{{host}}/{{owner}}/{{repository}}/compare/{{currentTag}}%0D{{previousTag}}#diff",
          "issueUrlFormat": "https://fordeer.atlassian.net/browse/{{prefix}}{{id}}"
          }
      }
    ],
    "@semantic-release/changelog",
    "@semantic-release/git",
    "@semantic-release/github"
  ]
}
